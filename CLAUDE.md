# CLAUDE.md

## プロジェクト概要
このプロジェクトは、イベント管理アプリのバックエンドAPIを Rails で実装し、
フロントエンドを Next.js で実装する構成です。

---

## 技術スタック
- Ruby 3.3.10
- Rails 7.1系（APIモード）
- PostgreSQL
- RSpec
- Docker Compose
- Next.js（フロントエンド）

---

## AIの振る舞い方針（重要）

- AIは単なる実装者ではなく、戦略的な協力者として振る舞うこと
- 指示をそのまま実行するのではなく、論理性・実用性の観点から妥当性を検討すること
- 問題がある場合は遠慮なく指摘し、理由と代替案を提示すること
- 不明点や前提の不足がある場合は、推測で進めず必ず確認すること
- 迎合せず、より良い設計・実装を優先すること

---

## 開発方針
- バックエンドAPIを正とする
- フロントエンドは API を利用して画面を構築する
- フロントエンド側で業務ロジックやDB前提の実装をしない
- まずはシンプルに実装し、必要に応じて整理・分離する
- 過度な抽象化は避ける
- 可読性を優先する

---

## ローカル開発
ローカル開発は Docker Compose を優先する。

よく使うコマンド:
- `docker compose up --build`
- `docker compose exec web bin/rails c`
- `docker compose exec web bundle exec rspec`

---

## 実装ルール
- Rails のコードはシンプルで読みやすく書く
- コントローラはできるだけ薄く保つ
- 業務ルールが明確な場合は model に寄せる
- リファクタ時は、明示的な指示がない限り挙動を変えない
- 不要なファイル追加や広範囲変更を避ける
- 既存のAPI契約を勝手に変更しない

---

## API設計ルール
- REST風のエンドポイント設計を基本とする
- エンドポイントは「HTTPメソッド + パス」で捉える
- クエリパラメータは絞り込み条件として扱う
- APIはJSONを返す
- 一覧系APIは配列で返す
- レスポンス形式はできるだけ一貫させる
- 関連データは必要な範囲でネストして返す

---

## 認証ルール
- 現在の認証は暫定的に `X-User-Id` ヘッダを利用する
- `X-User-Id` が未指定・不正・該当ユーザーなしの場合は `401 Unauthorized` を返す

---

## JSON設計ルール
- クライアントで使いやすい形で返す
- 必要な関連データは include する
- 不要に深いネストは避ける
- エラーレスポンスは簡潔にする

---

## エラーハンドリング
- 適切なHTTPステータスコードを返す
- エラーメッセージは簡潔にする
- エラー形式を変える場合は、関連するspecとdocsも更新する

---

## テスト方針
- API変更時は request spec を追加・更新する
- 正常系では以下を確認する
  - HTTPレスポンス
  - 重要なDBの状態変化
- 異常系では以下を確認する
  - HTTPステータス
  - エラーレスポンス
- 既存テストは明示的な指示がない限り削除しない

---

## DB / クエリ方針
- N+1 に注意する
- 一覧で関連データが必要な場合は `includes` を使う
- まずはシンプルに書き、必要に応じて最適化する

---

## フロントエンド実装ルール（重要）
- フロントエンドは Next.js で実装する
- フロントエンドは必ず API 経由でデータ取得・更新を行う
- フロントエンド側で新しいAPI仕様を勝手に決めない
- API仕様を追加・変更する場合は `docs/api.md` も更新する
- UI実装時は、まず既存APIで実現できるかを確認する
- 既存APIで不足がある場合のみ、バックエンド変更を提案する

---

## 変更ポリシー（重要）
- 既存の挙動は原則として変更しない
- 変更が必要な場合は、影響範囲を明示する
- リファクタリング時は「挙動が変わらないこと」を保証する
- 変更内容は小さく、意図が明確な単位に分ける

---

## Ruby / Docker整合性ルール（重要）
Ruby関連を変更する場合は以下を必ず一貫させる：

- `.ruby-version`
- `Gemfile`
- `Dockerfile`
- `Gemfile.lock`

不整合がある状態での変更は禁止

---

## 禁止事項（最重要）

以下の操作は、明示的な指示がない限り絶対に実行しないこと：

- git push / force push
- main / develop ブランチへの直接コミット
- データベースの drop / reset / migrate
- seed データの変更・実行
- 本番環境を想定した設定変更
- 既存データを破壊する可能性のある処理

変更が必要な場合は、必ず事前に提案し、承認を得てから実行すること

---

## 変更時の安全ルール
- 変更前に関連ファイルと既存specを確認する
- 影響範囲を把握してから実装する
- API変更時は必ずspecとdocsを更新する

---

## ブランチ運用
- `main` は安定版
- `develop` は開発統合ブランチ
- 作業は `develop` から作業ブランチを切る
- 原則として `main` に直接コミットしない

例：
- `feature/add-event-api`
- `fix/login-bug`

詳細は `docs/branching.md` を参照

---

## 参照ドキュメント
- `docs/api.md`
- `docs/setup.md`
- `docs/branching.md`
- `docs/tasks.md`
